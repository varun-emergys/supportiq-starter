# Slash Commands

This folder holds reusable Claude Code slash commands.

It starts empty on purpose — in **Exercise B1** you create a `/new-service`
command here that scaffolds a microservice (folders, API stub, Dockerfile).
