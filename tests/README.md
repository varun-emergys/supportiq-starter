# Tests

Starts empty on purpose. **Exercise A5** generates the first suite here —
unit tests for the ticket classifier covering every category and edge cases.

Run with:

    pytest

**Exercise B4** re-runs these same tests, unchanged, after swapping the
classifier internals — proving the module boundary held.
